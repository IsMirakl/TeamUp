# TeamUp Frontend

React + TypeScript (Vite) client for TeamUp.

## Run (Local)

```bash
cd frontend
npm install
npm run dev
```

- App: `http://localhost:5173`
- API base URL: `http://localhost:8080` (see `frontend/src/api/axiosConfig.ts`)

## Build

```bash
cd frontend
npm run build
```
